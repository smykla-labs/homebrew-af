# yaml-language-server: $schema=https://raw.githubusercontent.com/smykla-skalski/.github/main/schemas/sync-config.schema.json
---
sync:
  # Skip file synchronization for this repository
  files:
    skip: true

  # Enable label synchronization with removal of unlisted labels
  labels:
    allow_removal: true
